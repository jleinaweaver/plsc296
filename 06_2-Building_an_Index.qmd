---
format: revealjs
---

## Today's Agenda {background-image="Images/background-data_blue_v4.png" .center}

```{r}
library(tidyverse)
library(readxl)
library(kableExtra)
library(modelsummary)

d <- read_excel("../../DATA/2026-Spring/WDI/Week05-Univariate_Practice/SP2026-Wk05-WDI_Data.xlsx", na = "NA")

# library(RCPA3)
# anes <- nes
# save(anes, file = "../../DATA/2026-Spring/ANES/ANES2020_Extract-PollockEdwards.RData")

load(file = "../../DATA/2026-Spring/ANES/ANES2020_Data_Extract.RData")
```

<br>

::: {.r-fit-text}

**Creating, Transforming and Indexing Variables**

:::

- Building an index using the American National Election Survey (ANES) data

- Create a new R Script: "Building an additive index.R"

- Download the data" ANES2020_Data_Extract.RData

<br>

::: r-stack
Justin Leinaweaver (Spring 2026)
:::

::: notes
Prep for Class

1. Review Canvas submissions

2. NOTE for you when reviewing homework in class: Show your code for NEW tools and tasks, NOT old ones that are in their notes. So, show rescaling and recoding, not plotting!

3. Post ANES data from RCPA3 package on Canvas Modules page as Rdata so they can see the embedded questions in the data

<br>

**SLIDE**: Our work this week
:::



## {background-image="Images/background-data_blue_v4.png" .center}

**Univariate Analyses**

- Descriptive statistics (means, medians, modes, counts, SDs, etc)

- Visualizations (bar plots, box plots, histograms)

<br>

**This Week**

1. Modifying variables to help us analyze and present them

    - Rescaling (decimals and logs), creating dummies, creating ordinal variables

::: notes

Last class we learned a handful of R tools to allow you to modify variables to make univariate analyses more informative

- We rescaled variables by moving the decimal place,

- We rescaled variables using the log transformation to address right skewed data,

- We created dummy variables to focus on key parts of a distribution, and

- We converted interval variables into ordinal variables for ease of future analyses

<br>

**SLIDE**: Let's start by touching base on your practice assignment for today

:::



## {background-image="Images/background-data_blue_v4.png" .center .smaller}

::: {.r-fit-text}
**GNI per capita, Atlas method (current US$)**
:::

:::: {.columns}
::: {.column width="50%"}
```{r, fig.align='center', fig.retina=3, fig.asp=.9, fig.width=5, out.width="85%"}
d$gni_k <- d$`GNI per capita, Atlas method (current US$)`/1000

ggplot(data = d, aes(x = gni_k)) +
  geom_histogram(color = "white", bins = 15) +
  theme_bw() +
  labs(x = "GNI per capita (thousands current USD)", y = "Count")
```
:::

::: {.column width="50%"}
```{r, fig.align='center', fig.retina=3, fig.asp=.9, fig.width=5, out.width="85%"}
d$gni_log <- log(d$`GNI per capita, Atlas method (current US$)`)

ggplot(data = d, aes(x = gni_log)) +
  geom_histogram(color = "white", bins = 15) +
  theme_bw() +
  labs(x = "GNI per capita (log current USD)", y = "Count") +
  scale_x_continuous(breaks = 5:12)
```
:::
::::

```{r, echo=TRUE, eval=FALSE}
# Rescale by thousands
d$gni_k <- d$`GNI per capita, Atlas method (current US$)`/1000

# Rescale by log
d$gni_log <- log(d$`GNI per capita, Atlas method (current US$)`)
```

::: notes

*Q1 Compare a histogram of "GNI per capita, Atlas method (current US$)" in thousands of dollars to a second histogram using the logged version of the variable. Submit both histograms and explain what we learn about income in the world from these two views of the data.*

<br>

**Ok, what do we learn about income in the world from these two views of the data?**

- *REMEMBER, they can resubmit this for more points so let them lead the discussion of "what we learn".*

- *ALSO, only show code for the NEW tools, not the stuff in their notes already*

<br>

*My quick reflections (maybe not to share)*

- Useful to think about global development using two VERY DIFFERENT scales

- Most countries on the low end

- BUT there is quite a bit of variation between those countries

- AND small differences at the low end represent BIG differences in life experiences


```{r, echo=TRUE}
summary(d$gni_k)

summary(d$gni_log)
```

<br>

**SLIDE**: Q2

:::



## {background-image="Images/background-data_blue_v4.png" .center .smaller}

::: {.r-fit-text}
**Immunization, measles (% of children ages 12-23 months)**
:::

:::: {.columns}
::: {.column width="50%"}
```{r, fig.align='center', fig.retina=3, fig.asp=.9, fig.width=5}
ggplot(data = d, aes(x = `Immunization, measles (% of children ages 12-23 months)`)) +
  geom_histogram(color = "white", bins = 10) +
  theme_bw() +
  labs(x = "Immunization, measles (% of children ages 12-23 months)", y = "Count")
```
:::

::: {.column width="50%"}
```{r, fig.align='center', fig.retina=3, fig.asp=.9, fig.width=5}
d$measles_dummy <- 
  if_else(d$`Immunization, measles (% of children ages 12-23 months)` >= 95,
          "Herd Immunity", "At Risk")

ggplot(data = d, aes(x = measles_dummy)) +
  geom_bar(fill = c("red", "blue", "grey"), width = .6) +
  theme_bw() +
  labs(x = "Herd Immunity Requires Measles Immunizations >= 95%", y = "Count") +
  scale_x_discrete(limits = c("Herd Immunity", "At Risk", NA_character_))
```
:::
::::

```{r, echo=TRUE, eval=FALSE}
# Herd immunity dummy variable
d$measles_dummy <- 
  if_else(d$`Immunization, measles (% of children ages 12-23 months)` >= 95,
          "Herd Immunity", "At Risk")
```

::: notes

*Q2. Make a dummy variable that splits the WDI sample into two groups by level of measles immunization. Label all countries at or above 95% on "Immunization, measles (% of children ages 12-23 months)" as "Herd immunity" and the rest as "At risk". Submit a bar plot of your new variable and explain what we learn from this distribution.*

<br>

**Ok, what do we learn about income in the world from these two views of the data?**

- *REMEMBER, they can resubmit this for more points so let them lead the discussion of "what we learn".*

- *ALSO, only show code for the NEW tools, not the stuff in their notes already*

<br>

My prior assumption would be that those NAs are WAY more likely to be "at risk" BUT we SHOULD NOT make that assumption without making an argument!

- **Which countries / economies in the dataset are missing data for this year of measles immunizations?**

- (*Results at bottom*)

<br>

Given that these are tiny territories and most are not widely considered countries I feel much more comfortable excluding them from our analyses

- **What's the function we reviewed in Week 5 to change the order and which bars to include in a bar plot?**

- (scale_x_discrete)

<br>

**SLIDE**: Q3

<br>

```{r, echo=TRUE}
d$country[is.na(d$`Immunization, measles (% of children ages 12-23 months)`)]
```

:::



## {background-image="Images/background-data_blue_v4.png" .center .smaller}

::: {.r-fit-text}
**GNI per capita using the World Bank categories**
:::

:::: {.columns}
::: {.column width="50%"}
```{r, fig.align='center', fig.retina=3, fig.asp=.618, fig.width=5, out.width="60%"}
ggplot(data = d, aes(x = gni_k)) +
  geom_histogram(color = "white", bins = 10) +
  theme_bw() +
  labs(x = "GNI per capita, Atlas method (thousands current US$)", y = "Count")

ggplot(data = d, aes(x = gni_log)) +
  geom_histogram(color = "white", bins = 15) +
  theme_bw() +
  labs(x = "GNI per capita (log current USD)", y = "Count") +
  scale_x_continuous(breaks = 5:12)
```
:::

::: {.column width="50%"}
```{r, fig.align='center', fig.retina=3, fig.asp=.9, fig.width=5}
d$wb_income <- cut(d$`GNI per capita, Atlas method (current US$)`,
                   breaks = c(0, 1145, 4515, 14005, 135000),
                   labels = c("Low", "Low Middle", "Upper Middle", "High"))

ggplot(data = d, aes(x = wb_income)) +
  geom_bar(width = .6, fill = c("red4", "lightblue2", "lightblue3", "blue2")) +
  theme_bw() +
  labs(x = "World Bank Income Categories", y = "Count") +
  scale_x_discrete(limits = c("Low", "Low Middle", "Upper Middle", "High"))
```
:::
::::

```{r, echo=TRUE, eval=FALSE}
# GNI as an ordinal variable
d$wb_income <- cut(d$`GNI per capita, Atlas method (current US$)`,
                   breaks = c(0, 1145, 4515, 14005, 135000),
                   labels = c("Low", "Low Middle", "Upper Middle", "High"))
```

::: notes

*Q3. Replicate the "wb_income" variable in our WDI sample using the cut function and the data in "GNI per capita, Atlas method (current US$)". Submit the code you used to make the new variable and make an argument that you believe this ordinal variable is (or isn't) a "better" way to think about global development.*

*World Bank Gross National Income Categories for 2023*

- Low income (L) <= 1,145
- Lower middle income (LM) 1,146 - 4,515
- Upper middle income (UM) 4,516 - 14,005
- High income (H) > 14,005

<br>

**How did we do with the cut() function?**

- **Everybody get this to work?**

<br>

I omitted the NAs for the same rationale as in Q2

- e.g. tiny territories, most not widely considered "countries"

<br>

**SLIDE**: Back to the aims for this week

:::



## {background-image="Images/background-data_blue_v4.png" .center}

**Univariate Analyses**

- Descriptive statistics (means, medians, modes, counts, SDs, etc)

- Visualizations (bar plots, box plots, histograms)

<br>

**This Week**

1. Modifying variables to help us analyze and present them

    - Rescaling (decimals and logs), creating dummies, creating ordinal variables
    
2. Creating new variables to better measure our concepts

::: notes

Ok, so our work last class was meant to help you deepen your univariate analyses by modifying the variables you are exploring

<br>

**Just at a conceptual level, do you understand why you might do each of these things to a variable?**

<br>

**What about at the coding level, how are we doing with these R tools?**

<br>

The good news is that our work for the rest of the week will focus on these same tools from last class

- **SLIDE**: Our next step is to combine variables to make new ones!

:::



## {background-image="Images/background-data_blue_v4.png" .center}

::: {.r-fit-text}

**Why create an additive index of variables?**

:::: {.incremental}

1. May create a better estimate of a complex concept

2. Allows a wider array of analysis tools

3. May reduce random measurement error
:::
::::

::: notes

So, imagine you have a handful of variables that are all nominal or ordinal and focus on the same basic underlying concept

- We can use our tools from last class to combine them into a single, new interval variable

- There are at least three possible benefits to creating a new interval variable

<br>

**REVEAL**: Create better estimates of a concept

- Many concepts in political science are heavily contested and represent complex attitudes to the world (e.g. political ideology)

- No single question may be able to completely represent what you are interested in measuring in a valid AND reliable way

- In that case we can use multiple nominal or ordinal variables to measure all the related aspects of the concept

- THEN we can combine them into a single interval measure of the concept

<br>

**REVEAL**: Allows a wider array of analysis tools

- The second benefit relates to our univariate analysis tools

- There's simply way more tools available for analyzing an interval variable than a nominal varible

- e.g. mean, median, percentiles VS a frequency table

<br>

**REVEAL**: May reduce random measurement error

- In essence, each separate measure represents new information about the concept that were each measured with error

- The hope is that these errors, if they are truly random, will start to cancel each other out when we combine them

<br>

Important caveat to this last one

- Combining related measures measure helps with random error, NOT systematic error

- If your measures are invalid, combining them won't help

<br>

Alright let's do an exercise together so you can see how this works

- **SLIDE**: First our dataset!

:::



## {background-image="Images/background-data_blue_v4.png" .center .smaller}

![](Images/06_2-ANES_logo.png)

<br>

**To load the RData file in RStudio:**

1. "Session" &#8594; "Load Workspace..." &#8594; "ANES2020_Data_Extract.RData"

2. `View(anes)`

::: notes

For today I pointed you to a background reading on the American National Elections Survey (ANES)

- This is ABSOLUTELY a resource you should keep in mind if you plan on doing any research into the attitudes or opinions of the American public

- One of the best designed and frequently run academic surveys in existence

- The data is also freely available!

<br>

The extract of ANES we're working with today is an RData file instead of an Excel file

- The easiest way to load it is to use the steps on the slide in RStudio

- **Everybody have a data object called "anes" in the top-right window?**

<br>

Once you have the data loaded you should View it

- You'll see that the data structure of this RData file puts a longer variable description at the top of each column

- Very handy!

<br>

**SLIDE**: Ok, let's build an index

:::



## {background-image="Images/background-data_blue_v4.png" .center .smaller}

**ANES questions on Americans' "recent political activities" (within the last 12 months)**

- Gave money to a candidate (polact.givecand)

- Gave money to a political party (polact.giveparty)

- Gave money to another political organization (polact.giveoth)

- Posted a political yard sign or a bumper sticker (polact.postsign)

- Attended a political meeting in person (polact.meetings)

- Attended a political meeting online (polact.onlinemeet)

- Did other work for a political cause (polact.othwork)

- Talked about 2020 vote choice with family or friends (polact.talkvote)

- Talked about politics with family or friends (polact.talkpol)

::: notes

Let's say our interest is in measuring how "engaged" Americans are in the political process

- That's an incredibly difficult concept to define, so instead, the ANES asks a series of nine yes or no questions

- **Big picture, do we accept that these are nine valid aspects of the concept "engagement"? Why or why not?**

<br>

**SLIDE**: To learn the process of combining nominal variables let's focus on just three of the variables

:::



## {background-image="Images/background-data_blue_v4.png" .center .smaller}

**ANES questions on Americans' "recent political activities" (within the last 12 months)**

- Gave money to a candidate (polact.givecand)

- Gave money to a political party (polact.giveparty)

- Gave money to another political organization (polact.giveoth)

::: {.fragment}
```{r, echo=TRUE}
table(anes$polact.givecand)

table(anes$polact.giveparty)

table(anes$polact.giveoth)
```
:::

::: notes

To start, let's describe these three variables

- **What tool do we use to analyze a nominal variable?** (frequency table)

<br>

Ok, make a frequency table for each of these three variables

- (**REVEAL**)

<br>

**What do we learn from these frequency tables?**

1. (All the nominal variables appear to be coded the same way)

2. (Most people don't do these three things, but political donations are highest)

<br>

ALWAYS do a univariate analysis BEFORE anything more complicated

- You have to know what each variable looks like before you start messing with the data!

<br>

**SLIDE**: Ok, building our index requires using the if_else function

:::



## {background-image="Images/background-data_blue_v4.png" .center}

**Building an Index (using nominal variables)**

- Convert the variables into numeric dummies

```{r, echo=TRUE, eval=FALSE}
# Gave money to a candidate
anes$polact.givecand
```

```{r, eval=TRUE}
anes$polact.givecand[1:30]
```

<br>

```{r, echo=TRUE, eval=FALSE}
# Gave money to a candidate
if_else(anes$polact.givecand == "1. Yes", 1, 0)
```

```{r, eval=TRUE}
if_else(anes$polact.givecand == "1. Yes", 1, 0)[1:35]
```

::: notes

Here I'm focusing on just the first of the nine variables and I'm showing you what you already know, which is how to use the if_else() function

- In order to add together multiple variables we need a version of each one made of 1s and 0s, not "yes" and "no"

- **Is everybody clear on what if_else is doing here?**

<br>

**SLIDE**: Now we can build the index

:::



## {background-image="Images/background-data_blue_v4.png" .center}

**Building an Index (using nominal variables)**

- Convert the dummies, sum them, AND save as a new variable

```{r, echo=TRUE, eval=TRUE}
anes$engagement_index <- 
  if_else(anes$polact.givecand == "1. Yes", 1, 0) +
  if_else(anes$polact.giveparty == "1. Yes", 1, 0) +
  if_else(anes$polact.giveoth == "1. Yes", 1, 0)
```

<br>

```{r, echo=TRUE, eval=TRUE}
table(anes$engagement_index)

proportions(table(anes$engagement_index))
```

::: notes

*Step through code*

- Everybody make sure you get the same results in the table

<br>

**What do we learn about Americans' political engagement in 2020 from this index?**

<br>

Per this specific index:

- (78% of the sample is not engaged in politics)

- 21% did at least one engagement activity in the last 12 months

- 3% did all three!

<br>

**Questions on building an index from nominal variables?**

<br>

**SLIDE**: Now let's build an index by combining multiple ordinal variables

:::



## {background-image="Images/background-data_blue_v4.png" .center}

**ANES questions on Americans' "racial attitudes"**

- Have concerned feelings for other racial/ethnic groups (anes$oth.race.concern)

- Imagine how they would feel before criticizing other groups (anes$oth.race.feel)

- Try to understand the perspective of other racial/ethnic groups (anes$oth.race.persp)

- Feel protective of someone due to race or ethnicity (anes$oth.race.protect)

::: notes

The ANES also asks these four questions in order to gauge Americans' "racial attitudes"

- Each variable is measured on a scale of 1-5

- From "1" meaning "Extremely often" up to "5" meaning "Not often at all"

<br>

Everybody check the frequency table for these four variables

- (**SLIDE**)

:::




## {background-image="Images/background-data_blue_v4.png" .center .smaller}

::: {.r-fit-text}
**ANES questions on Americans' "racial attitudes"**
:::

<br>

```{r, echo=FALSE, fig.align='center', fig.asp=.5, fig.width=11}
rbind("oth.race.concern" = proportions(table(anes$oth.race.concern)),
      "oth.race.feel" = proportions(table(anes$oth.race.feel)),
      "oth.race.persp" = proportions(table(anes$oth.race.persp)),
      "oth.race.protect" = proportions(table(anes$oth.race.protect))) |>
  kable(digits = 2) |>
  kable_styling(font_size = 25)

# anes |>
#   select(oth.race.concern, oth.race.feel, oth.race.persp, oth.race.protect) |>
#   pivot_longer(cols = c(oth.race.concern, oth.race.feel, oth.race.persp, oth.race.protect), names_to = "attitudes", values_to = "values") |>
#   ggplot(aes(y = values)) +
#   geom_bar() +
#   theme_bw() +
#   labs(x = "", y = "") +
#   facet_wrap(~ attitudes, ncol = 4)

```

::: notes

I think the proportions are MUCH easier to interpret when the sample size is huge

<br>

**What do we learn from these frequency tables?**

<br>

**SLIDE**: Ok, let's learn to build an additive index when the input variables are ordinal instead of nominal

:::



## {background-image="Images/background-data_blue_v4.png" .center}

**Building an Index (using ordinal variables)**

- Convert the variables into numbers

```{r}
options(width = 80)
```

```{r, echo=TRUE, eval=FALSE}
anes$oth.race.concern
```

```{r, eval=TRUE}
anes$oth.race.concern[1:9]
```

<br>

::: {.fragment}
```{r, echo=TRUE, eval=TRUE}
levels(anes$oth.race.concern)
```
:::

<br>

::: {.fragment}
```{r, echo=TRUE, eval=FALSE}
as.numeric(anes$oth.race.concern)
```

```{r, eval=TRUE}
as.numeric(anes$oth.race.concern)[1:35]
```
:::
::: notes

Let's start by looking at the values in the first variable

- As we noted before, each of the racial attitudes questions has five possible answers

- Ideally, we'd like to preserve all of this variation in the measurement and translate it into the index

- In other words, the new index should recognize that a respondent with a "Not too often" score is different from one with a "somewhat different" response

<br>

The good news is that our version of the ANES dataset has coded all of the ordinal variables as "ordered factors"

- That means that each variable records the observations AND it knows how to order the possible responses to each question

<br>

**REVEAL**: We can see this with the levels() function

- IF a variable is coded as a factor, this shows us the levels and their order

- We'll learn to convert ordinal variables into ordered factors in R later in the semester

<br>

**REVEAL**: SINCE the order is already built-in here, all we have to do is use the as.numeric function to translate the levels into their numeric order

- This function converts the variable into numbers based on the levels of the variable

<br>

**Any questions on the code here?**

<br>

**SLIDE**: Once we have our variables as numbers, combining them is super easy!

:::



## {background-image="Images/background-data_blue_v4.png" .center}

**Building an Index (using ordinal variables)**

- Convert the variable to a factor, convert the factor levels to numbers, sum them, AND save as a new variable

```{r, echo=TRUE, eval=TRUE}
anes$racial_attitudes <- as.numeric(anes$oth.race.concern) +
  as.numeric(anes$oth.race.feel)
```

<br>

```{r, echo=TRUE, eval=TRUE}
table(anes$racial_attitudes)

proportions(table(anes$racial_attitudes))
```

::: notes

As I noted, we'll learn to make factors later (maybe next class?)

- For now we move forward with the index process using variables that are already factors

<br>

*Step through code*

- Everybody make sure you get the same results in the table

<br>

**What do we learn about Americans' racial attitudes in 2020 from this index?**

- BE CAREFUL interpreting this as HIGHER numbers mean less tolerance!

<br>

**SLIDE**: Wrapping up

:::



## {background-image="Images/background-data_blue_v4.png" .center}

**Univariate Analyses**

- Descriptive statistics (means, medians, modes, counts, SDs, etc)

- Visualizations (bar plots, box plots, histograms)

<br>

**This Week**

1. Modifying variables to help us analyze and present them

2. Creating new variables to better measure our concepts

::: notes

Let's wrap up by touching base on our work this week

- Remember, our aim is to deepen our univariate analyses by modifying and/or creating new variables

<br>

**Is everybody clear on the concepts involved?**

- **e.g. why modifying and creating variables helps us with univariate analysis?**

<br>

- **Is everybody clear on the code being used?**

<br>

**SLIDE**: For next class

:::



## For Next Class {background-image="Images/background-data_blue_v4.png" .center}

<br>

1. Finish the two indicies we started in class today (prompts on Canvas)

2. Read p824-830 in Miller, Saunders and Farhart (2016)

3. Review the political knowledge and conspiracy theory questions

::: notes


Canvas Assignment: Practice Building and Evaluating an Index

1. Construct an additive index of Americans' engagement with the political process using all nine variables we discussed in class (polact.givecand, polact.giveparty, polact.giveoth, polact.postsign, polact.meetings, polact.onlinemeet, polact.othwork, polact.talkvote, polact.talkpol). Submit a polished visualization of your constructed index AND your analysis of the results. Assuming this is a representative sample of Americans, how engaged were we in the political process in 2020? 

2. Construct an additive index of Americans' "racial attitudes" using the four variables we discussed in class (oth.race.concern, oth.race.feel, oth.race.persp, oth.race.protect). Submit a polished visualization of your constructed index AND your analysis of the results. Assuming this is a representative sample of Americans, how would you describe the racial attitudes of voters in 2020?


:::






